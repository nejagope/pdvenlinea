{% extends '::base.html.twig'%}
{% block body %}
	{% block menu %}
	<div class="navbar navbar-default">
      <div class="container">
        <div class="navbar-header">
          <a href="{{ path('pdv_homepage') }}" class="navbar-brand">
          	<span class="glyphicon glyphicon-home"></span> PDV En Línea({{ app.user.username }})</a>
          <button class="navbar-toggle" type="button" data-toggle="collapse" data-target="#navbar-main">
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
        </div>
        <div class="navbar-collapse collapse" id="navbar-main">
          <ul class="nav navbar-nav">
            
            <li class="dropdown">
              <a class="dropdown-toggle" data-toggle="dropdown" href="#" id="themes"> <span class="glyphicon glyphicon-cog"></span> Mantenimientos <span class="caret"></span></a>
              <ul class="dropdown-menu" aria-labelledby="themes">
              	<li><a href="{{ path('cliente') }}">Clientes</a></li>
                <li><a href="{{ path('producto') }}">Productos</a></li>
                {% if is_granted('ROLE_ADMIN') %}
                  <li><a href="{{ path('fabricante') }}">Fabricantes</a></li>
                  <li><a href="{{ path('unidadesmedida') }}">Unidades de Medida</a></li>
                  <li><a href="{{ path('tipomoneda') }}">Tipos de Moneda</a></li>
                  <li><a href="{{ path('categoriaproducto') }}">Categorias de Producto</a></li>
                  <li><a href="{{ path('empresa') }}">Empresas</a></li>
                {% endif %}
              </ul>
            </li>
            {% if is_granted('ROLE_ADMIN') %}
            <li class="dropdown">
              <a class="dropdown-toggle" data-toggle="dropdown" href="#" id="themes"><span class="glyphicon glyphicon-stats"></span> Reportes <span class="caret"></span></a>
              <ul class="dropdown-menu" aria-labelledby="themes">
                <li><a href="{{ path('ventasdiarias')}}">Ventas Diarias</a></li>
                <li><a href="{{ path('ventasporvendedor')}}">Ventas por Vendedor</a></li>
                <li><a href="{{ path('productosmasvendidos')}}">Productos más vendidos</a></li>
                <li><a href="{{ path('clientescompras')}}">Clientes que más compran</a></li>
              </ul>
            </li>
            {% endif %}
             <li class="dropdown">
              <a class="dropdown-toggle" data-toggle="dropdown" href="#" id="themes"><span class="glyphicon glyphicon-th-list"></span> Operaciones <span class="caret"></span></a>
              <ul class="dropdown-menu" aria-labelledby="themes">
                <li><a href="{{ path('factura') }}">Facturación</a></li>
              </ul>
            </li>
          </ul>

          <ul class="nav navbar-nav navbar-right">
            <li><a href="{{ path('logout') }}"><span class="glyphicon glyphicon-off"></span>  Salir</a></li>
          </ul>

        </div>
      </div>
    </div>
    {% endblock %}
    {% block content %}

    {% endblock %}
{% endblock %}